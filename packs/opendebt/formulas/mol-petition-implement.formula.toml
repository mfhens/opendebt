description = """
Phase 2 of the OpenDebt petition pipeline.

Implements approved scaffold artefacts under strict TDD discipline:
  Playwright test generation (RED) → TDD implementation → E2E acceptance (optional)
  → code review → [Catala encoding, Tier A only]
  → HUMAN GATE: review code before merge
  → documentation sync
  → HUMAN GATE: final merge approval

Aligns with Claude `pipeline-conductor` Phase 5–6.5 where applicable: failing
Playwright tests in `opendebt-e2e/`, then Java/Cucumber TDD, then VAL-* E2E
when `validation-contract.md` exists.

This formula assumes mol-petition-scaffold has completed and the human
scaffold gate has been closed (approved).

## Prerequisites (set by Phase 1)
- `metadata.outcome_contract_path` — outcome contract file path
- `metadata.spec_path` — implementation spec file path
- `metadata.validation_contract_path` — optional; `petitions/validation/<id>/validation-contract.md` for E2E
- `metadata.catala_tier` — A, B, or C (determines whether catala-encode step runs)

## Human gates
Two human gates are mandatory for regulated-domain compliance:

Gate 1 (human-review-code): After code review passes — approve before Catala/doc-sync.
  bd close <bead_id> "Approved"

Gate 2 (human-merge-gate): After doc-sync — approve to merge the feature branch.
  bd close <bead_id> "Merge approved"

## Branch model
The tdd-enforcer creates a feature branch:
  feature/{{petition_id}}-<service>

After merge gate approval, open a PR manually or close the gate to trigger merge:
  gh pr create --base main --head feature/{{petition_id}}-<service>
"""
formula = "mol-petition-implement"
version = 3

[vars]
[vars.petition_id]
description = "Beads ID of the petition"
required = true

[vars.service]
description = "Target service name (e.g. rules-engine, debt-service, creditor-portal)"
required = true

[vars.catala_tier]
description = "Tier A/B/C — Tier A triggers catala-encode step"
default = "B"

[vars.implementer]
description = "Rig-scoped agent for implementation: tdd-enforcer (backend) or portal-tdd-enforcer (portals)"
default = "tdd-enforcer"

# ── Steps ────────────────────────────────────────────────────────────────────

[[steps]]
id = "test-generate"
title = "Generate failing Playwright E2E tests for {{petition_id}}"
assignee = "opendebt/playwright-test-generator"
needs = []
description = """
Generate native Playwright TypeScript `test()` blocks from the petition Gherkin feature,
outcome contract, and implementation spec — RED tests that fail until the stack is ready.

**Canonical instructions:** read `~/.claude/agents/playwright-test-generator.agent.md`.

**Config:** `.factory/project.yaml` — `test_framework: playwright`, `e2e.project_root` (`opendebt-e2e/`).

**Inputs:**
- Feature file: `opendebt-{{service}}/src/test/resources/features/{{petition_id}}-*.feature`
- Outcome contract + implementation spec paths from petition bead metadata
- If the repo also maintains `petitions/{{petition_id}}/{{petition_id}}.feature`, prefer that as the single Gherkin source of truth when present

**Commands (sanity):**
```bash
cd opendebt-e2e && npm ci && npx playwright test --list
```

**Close:**
```bash
bd close {{issue}} "Playwright RED tests generated under opendebt-e2e/tests/"
```
"""

[[steps]]
id = "implement"
title = "TDD implementation for {{petition_id}} in {{service}}"
assignee = "{{service}}/{{implementer}}"
needs = ["test-generate"]
description = """
Implement Java code for {{petition_id}} under strict TDD discipline.

**Working directory:** `opendebt-{{service}}/`

**Before starting — verify baseline:**
```bash
cd opendebt-{{service}}
mvn test -q
```
All existing tests must pass. If they do not, stop and notify opendebt/code-reviewer.

The **test-generate** step should already have produced failing Playwright tests under
`opendebt-e2e/`; keep Cucumber/Java TDD as the primary service-level loop.

**1. Create feature branch:**
```bash
git checkout main && git pull origin main
git checkout -b feature/{{petition_id}}-{{service}}
bd update {{petition_id}} --set-metadata branch="feature/{{petition_id}}-{{service}}"
```

**2. Read your inputs:**
```bash
SPEC=$(bd show {{petition_id}} --json | jq -r '.metadata.spec_path')
cat "$SPEC"
ls opendebt-{{service}}/src/test/resources/features/ | grep {{petition_id}}
```

**3. TDD cycle — for each acceptance criterion:**
a. Write/complete the JUnit unit test (the step stub already exists from Phase 1)
b. Run `mvn test -pl opendebt-{{service}} -q` — confirm it FAILS (red)
c. Write the minimum production code to make it pass
d. Run `mvn test -pl opendebt-{{service}} -q` — confirm it PASSES (green)
e. Refactor if needed, re-run to confirm still green

**Package structure (mandatory):**
```
dk.ufst.opendebt.<service>/
├── config/       Spring configuration
├── controller/   REST controllers
├── service/      Business logic interfaces
│   └── impl/     Implementations
├── client/       API clients for other services
├── dto/          DTOs
├── mapper/       MapStruct mappers
└── exception/    Custom exceptions
```

**GDPR constraints (critical):**
- Never store CPR/CVR/name/address in this service's entities
- Cross-service data: use UUID `person_id` references only
- Cross-service calls: use API clients in `client/` package

**4. Full verification:**
```bash
mvn verify -pl opendebt-{{service}}
mvn spotless:check -pl opendebt-{{service}}
```
Both must pass. Fix any failures before proceeding.

**5. JaCoCo coverage check:**
```bash
# Coverage is enforced by maven-enforcer-plugin in mvn verify
# If coverage < 80% line / 70% branch, add tests before proceeding
```

**6. Commit:**
```bash
git add opendebt-{{service}}/src/
git commit -m "feat({{petition_id}}): implement {{petition_id}} in {{service}}"
```

**7. Push and close:**
```bash
git push origin feature/{{petition_id}}-{{service}}
bd close {{issue}} "Implementation complete, branch pushed"
```
"""

[[steps]]
id = "e2e-acceptance"
title = "E2E acceptance (Playwright VAL-*) for {{petition_id}}"
assignee = "opendebt/user-testing-flow-validator"
needs = ["implement"]
description = """
Run Playwright E2E assertions from `validation-contract.md` against a running stack
(Phase 6.5 in `pipeline-conductor`).

**Canonical instructions:** `~/.claude/agents/user-testing-flow-validator.agent.md`.

**Skip this step** (close immediately) if ANY of:
- `.factory/library/user-testing.md` (or project-equivalent) is absent
- `petitions/validation/{{petition_id}}/validation-contract.md` is absent
- No VAL-* assertions apply to this petition

```bash
bd close {{issue}} "Skipped: no validation contract or user-testing library for E2E"
```

**When running:** use `playwright-cli` skill; write evidence to
`petitions/validation/{{petition_id}}/user-testing/flows/main.json`; pass assertion IDs
from the validation contract. Base URLs: `.factory/project.yaml` `e2e.base_urls`.

**On failure:** reopen **implement** with failing assertion IDs — max one retry cycle, then escalate.

**Close when green:**
```bash
bd close {{issue}} "E2E VAL assertions passed; evidence under petitions/validation/{{petition_id}}/user-testing/"
```
"""

[[steps]]
id = "review"
title = "Code review for {{petition_id}}"
assignee = "opendebt/code-reviewer"
needs = ["e2e-acceptance"]
description = """
Strict code review against petition intent, implementation spec, and opendebt standards.

**1. Read review inputs:**
```bash
git fetch origin
git checkout feature/{{petition_id}}-{{service}}
SPEC=$(bd show {{petition_id}} --json | jq -r '.metadata.spec_path')
cat "$SPEC"
```

**2. Review checklist:**
- [ ] Implementation matches spec — no under-delivery, no over-engineering
- [ ] Package structure matches `dk.ufst.opendebt.<service>/` convention
- [ ] No PII stored outside person-registry — UUID references only
- [ ] No cross-service DB access — only API clients in `client/` package
- [ ] Audit trail via `audit-trail-commons` for state-changing operations
- [ ] All Gherkin scenarios in `features/{{petition_id}}-*.feature` pass
- [ ] `mvn verify` passes (unit + integration tests, JaCoCo ≥80%/70%)
- [ ] `mvn spotless:check` clean

**3. Run security scan (mandatory before approving):**
Use the `snyk_code_scan` MCP tool if available in this session.
Rescan after any fixes.

**4a. If changes needed:**
```bash
bd update {{issue}} \
  --notes "Review failed: <specific issues>" \
  --status open \
  --assignee {{service}}/tdd-enforcer
```
The tdd-enforcer will pick it up and fix the issues.

**4b. If approved:**
```bash
bd close {{issue}} "Code review passed"
```
"""

[[steps]]
id = "catala-encode"
title = "Catala encoding for {{petition_id}} (Tier A)"
assignee = "opendebt/catala-encoder"
needs = ["review"]
description = """
Encode the statutory rule from {{petition_id}} in Catala formal specification.
This step only runs for Tier A petitions ({{catala_tier}}).
If catala_tier is B or C, this step can be skipped — close it immediately.

**For Tier B/C — skip:**
```bash
bd close {{issue}} "Skipped: catala_tier={{catala_tier}}"
```

**For Tier A — encode:**

**1. Read the statutory rule source from the petition:**
```bash
cat petitions/{{petition_id}}/{{petition_id}}.md | grep -A 50 "## Legal reference"
```

**2. Produce Catala source file:**
Path: `catala/{{petition_id}}-rule.catala_en`

Structure:
```catala
# Title: <rule name>
# Source: <juridisk vejledning reference>

scope <RuleName>:
  input ...: <type>
  output ...: <type>
  rule ...:
    definition ... equals ...
    under condition ... consequence ...
```

**3. Produce companion test scope:**
Path: `catala/{{petition_id}}-test.catala_en`

**4. Typecheck and interpret:**
```bash
catala typecheck --language en --no-stdlib catala/{{petition_id}}-rule.catala_en
catala interpret --language en --no-stdlib --scope TestScope catala/{{petition_id}}-test.catala_en
```
Both must pass without errors.

**5. Verify CI Catala gate will pass:**
The CI job `catala-tests` typechecks all files matching `catala/*.catala_en`.
New files are automatically included — no CI config change needed.

**6. Commit:**
```bash
git add catala/
git commit -m "catala({{petition_id}}): add formal encoding for statutory rule"
git push origin feature/{{petition_id}}-{{service}}
```

**7. Close:**
```bash
bd close {{issue}} "Catala encoding complete and CI-verified"
```
"""

[[steps]]
id = "human-review-code"
title = "HUMAN GATE: Review implementation for {{petition_id}} before merge"
assignee = "human"
needs = ["catala-encode"]
description = """
Human review before merge. This gate is mandatory for regulated-domain compliance.

**Branch:** feature/{{petition_id}}-{{service}}
**Catala tier:** {{catala_tier}}

**Review checklist:**
- [ ] `mvn verify` passes on the feature branch
- [ ] Code review passed (step above closed)
- [ ] GDPR constraints respected (no PII outside person-registry)
- [ ] For Tier A: Catala encoding matches the Java implementation
- [ ] No accidental scope creep beyond the petition

**To check CI status:**
```bash
gh run list --branch feature/{{petition_id}}-{{service}} --limit 5
```

**To approve (triggers doc-sync):**
```bash
bd close <this_bead_id> "Approved for merge"
```

**To reject:**
```bash
bd update <this_bead_id> --notes "<reason>" \
  --assignee {{service}}/tdd-enforcer --status open
```
"""

[[steps]]
id = "doc-sync"
title = "Documentation sync for {{petition_id}}"
assignee = "opendebt/doc-sync"
needs = ["human-review-code"]
description = """
Update documentation after approved implementation.
Commit all doc changes to the same feature branch as the code.

**1. Check out the feature branch:**
```bash
git fetch origin
git checkout feature/{{petition_id}}-{{service}}
```

**2. Update (only sections that are actually affected):**

- `architecture/overview.md` — service inventory, endpoint lists, implementation status table
- `agents.md` — if any new agent patterns were discovered during this petition
- Relevant ADR in `architecture/adr/` — if an architectural decision was implicit in the implementation
- `docs/site/technical/` — developer guide, API reference (if REST endpoints changed)
- `docs/site/fordringshaver/` — creditor guide (if creditor-facing behaviour changed)
- `docs/site/skyldner/` — citizen guide (if citizen-facing behaviour changed)
- `docs/site/sagsbehandler/` — caseworker guide (if caseworker-facing behaviour changed)

**3. Build and verify MkDocs:**
```bash
mkdocs build --strict 2>&1 | tail -20
```
Fix any broken links or missing references before proceeding.

**4. Update program-status.yaml:**
```bash
# Change the petition status from 'in_progress' to 'implemented'
# in petitions/program-status.yaml
```

**5. Commit:**
```bash
git add architecture/ agents.md docs/ petitions/program-status.yaml
git commit -m "docs({{petition_id}}): sync documentation after implementation"
git push origin feature/{{petition_id}}-{{service}}
```

**6. Close:**
```bash
bd close {{issue}} "Documentation synced and committed"
```
"""

[[steps]]
id = "human-merge-gate"
title = "HUMAN GATE: Final merge approval for {{petition_id}}"
assignee = "human"
needs = ["doc-sync"]
description = """
Final human gate. Close this step to approve the feature branch for merge.

**Branch:** feature/{{petition_id}}-{{service}}

All automated steps are complete. The branch contains:
- Production code (red → green → refactor)
- Gherkin scenarios passing
- Catala encoding (if Tier A)
- Documentation updates
- program-status.yaml updated to 'implemented'

**To open a PR (recommended for traceability):**
```bash
gh pr create \
  --base main \
  --head feature/{{petition_id}}-{{service}} \
  --title "feat({{petition_id}}): <petition title>" \
  --body "Closes petition {{petition_id}}"
```

**To approve and merge directly:**
```bash
bd close <this_bead_id> "Merge approved"
git checkout main && git merge --no-ff feature/{{petition_id}}-{{service}}
git push origin main
git branch -d feature/{{petition_id}}-{{service}}
git push origin --delete feature/{{petition_id}}-{{service}}
```

**After merge — update Beads:**
```bash
bd update {{petition_id}} --status closed
bd dolt push
```
"""
