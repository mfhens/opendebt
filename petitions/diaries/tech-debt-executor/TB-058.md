# Diary: tech-debt-executor - TB-058
**Date**: 2026-04-12
**Item**: Wire fordring Drools validation into claim-ingestion paths
**Status**: done
**Pattern source**: `opendebt-debt-service/src/main/java/dk/ufst/opendebt/debtservice/config/DroolsAutoConfiguration.java`

## Key Findings
- FACT: debt-service already had the shared KieContainer wiring pattern from TB-057, so TB-058 only needed a parallel `FordringValidationService` bean plus an adapter layer.
- DECISION: creditor-portal and integration-gateway now send `X-OpenDebt-Claim-Ingress-Path` so debt-service can map portal vs system-to-system submissions onto one Drools contract without introducing endpoint forks.
- DEVIATION: do not leave raw `POST /api/v1/debts` open to creditor-facing callers once `/api/v1/debts/submit` is the validated ingestion path.
- LEARNED: a lightweight SpringBootTest with real Drools evaluation is enough to prove AFVIST/UDFOERT outcomes, but JaCoCo still logs non-fatal instrumentation noise from Drools lexer classes during startup.
