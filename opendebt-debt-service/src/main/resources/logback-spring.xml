<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProfile name="!dev">
        <!-- Production / default: structured JSON logging with trace correlation -->
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>spanId</includeMdcKeyName>
                <fieldNames>
                    <timestamp>@timestamp</timestamp>
                    <version>[ignore]</version>
                </fieldNames>
                <customFields>{"service":"opendebt-debt-service"}</customFields>
            </encoder>
        </appender>
    </springProfile>

    <springProfile name="dev">
        <!-- Dev profile: human-readable console output with trace IDs -->
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} [traceId=%X{traceId} spanId=%X{spanId}] - %msg%n</pattern>
            </encoder>
        </appender>
    </springProfile>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

    <logger name="dk.ufst.opendebt" level="INFO" />
    <logger name="org.springframework.security" level="INFO" />

</configuration>
