package dk.ufst.opendebt.debtservice.entity;

/** PSRM fordringsart — whether a claim is for collection or offsetting. */
public enum FordringsartEnum {
  /** Inddrivelse (collection). */
  INDR,
  /** Modregning (offsetting). */
  MODR
}
