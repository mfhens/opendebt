apiVersion: v1
kind: Service
metadata:
  name: opendebt-case-service
  namespace: opendebt
  labels:
    app: opendebt-case-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: opendebt-case-service
