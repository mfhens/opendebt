apiVersion: v1
kind: Service
metadata:
  name: opendebt-person-registry
  namespace: opendebt
  labels:
    app: opendebt-person-registry
spec:
  type: ClusterIP
  ports:
    - port: 8090
      targetPort: 8090
      protocol: TCP
      name: http
  selector:
    app: opendebt-person-registry
