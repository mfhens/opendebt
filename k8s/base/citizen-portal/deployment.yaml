apiVersion: apps/v1
kind: Deployment
metadata:
  name: opendebt-citizen-portal
  namespace: opendebt
  labels:
    app: opendebt-citizen-portal
    app.kubernetes.io/name: opendebt-citizen-portal
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: opendebt
spec:
  replicas: 2
  selector:
    matchLabels:
      app: opendebt-citizen-portal
  template:
    metadata:
      labels:
        app: opendebt-citizen-portal
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8086"
        prometheus.io/path: "/borger/actuator/prometheus"
    spec:
      serviceAccountName: opendebt-service-account
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: citizen-portal
          image: opendebt/opendebt-citizen-portal:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8086
              protocol: TCP
          envFrom:
            - configMapRef:
                name: opendebt-config
            - secretRef:
                name: opendebt-secrets
          env:
            - name: TASTSELV_ISSUER_URI
              valueFrom:
                secretKeyRef:
                  name: opendebt-secrets
                  key: TASTSELV_ISSUER_URI
            - name: TASTSELV_AUTH_URI
              valueFrom:
                secretKeyRef:
                  name: opendebt-secrets
                  key: TASTSELV_AUTH_URI
            - name: TASTSELV_TOKEN_URI
              valueFrom:
                secretKeyRef:
                  name: opendebt-secrets
                  key: TASTSELV_TOKEN_URI
            - name: TASTSELV_USERINFO_URI
              valueFrom:
                secretKeyRef:
                  name: opendebt-secrets
                  key: TASTSELV_USERINFO_URI
            - name: TASTSELV_JWK_URI
              valueFrom:
                secretKeyRef:
                  name: opendebt-secrets
                  key: TASTSELV_JWK_URI
            - name: TASTSELV_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: opendebt-secrets
                  key: TASTSELV_CLIENT_SECRET
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /borger/actuator/health/liveness
              port: 8086
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /borger/actuator/health/readiness
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: opendebt-citizen-portal
                topologyKey: kubernetes.io/hostname
