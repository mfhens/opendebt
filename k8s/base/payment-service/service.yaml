apiVersion: v1
kind: Service
metadata:
  name: opendebt-payment-service
  namespace: opendebt
  labels:
    app: opendebt-payment-service
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
  selector:
    app: opendebt-payment-service
