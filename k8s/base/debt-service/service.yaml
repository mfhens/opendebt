apiVersion: v1
kind: Service
metadata:
  name: opendebt-debt-service
  namespace: opendebt
  labels:
    app: opendebt-debt-service
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: opendebt-debt-service
