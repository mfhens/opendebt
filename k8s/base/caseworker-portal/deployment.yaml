apiVersion: apps/v1
kind: Deployment
metadata:
  name: opendebt-caseworker-portal
  namespace: opendebt
  labels:
    app: opendebt-caseworker-portal
    app.kubernetes.io/name: opendebt-caseworker-portal
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: opendebt
spec:
  replicas: 2
  selector:
    matchLabels:
      app: opendebt-caseworker-portal
  template:
    metadata:
      labels:
        app: opendebt-caseworker-portal
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8087"
        prometheus.io/path: "/caseworker-portal/actuator/prometheus"
    spec:
      serviceAccountName: opendebt-service-account
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: caseworker-portal
          image: opendebt/opendebt-caseworker-portal:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8087
              protocol: TCP
          envFrom:
            - configMapRef:
                name: opendebt-config
            - secretRef:
                name: opendebt-secrets
          env:
            - name: KEYCLOAK_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: opendebt-secrets
                  key: KEYCLOAK_CASEWORKER_PORTAL_CLIENT_SECRET
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /caseworker-portal/actuator/health/liveness
              port: 8087
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /caseworker-portal/actuator/health/readiness
              port: 8087
            initialDelaySeconds: 30
            periodSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: opendebt-caseworker-portal
                topologyKey: kubernetes.io/hostname
