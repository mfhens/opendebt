apiVersion: v1
kind: Service
metadata:
  name: opendebt-creditor-portal
  namespace: opendebt
  labels:
    app: opendebt-creditor-portal
spec:
  type: ClusterIP
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
      name: http
  selector:
    app: opendebt-creditor-portal
